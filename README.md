# 🛒 MarketPlace

Уеб приложение за онлайн пазар, където потребителите могат да публикуват и търсят обяви за различни категории стоки.

## 📋 Описание

MarketPlace е пълнофункционална платформа за купуване и продаване, която позволява на потребителите да:
- Преглеждат каталог с обяви
- Публикуват собствени обяви
- Добавят обяви в любими
- Коментират обяви
- Управляват своя профил

## 🏗️ Архитектура

Проектът е разделен на две основни части:

### 1. **Client (Frontend)** - React приложение
- Използва React 18 с React Router за навигация
- Material-UI за дизайн компоненти
- Vite като build инструмент
- Работи на `http://localhost:8080` (по подразбиране)

### 2. **Server (Backend)** - Node.js сървър
- RESTful API сървър
- JSON файлове за съхранение на данни
- Работи на `http://localhost:3030`
- Admin панел на `http://localhost:3030/admin`

## 🚀 Как да стартираш проекта

### Първо: Стартирай сървъра

```bash
cd MarketPlace/server
node server.js
```

Сървърът ще започне на порт 3030. Ще видиш съобщение:
```
Server started on port 3030. You can make requests to http://localhost:3030/
Admin panel located at http://localhost:3030/admin
```

### Второ: Стартирай клиента

Отвори нов терминал:

```bash
cd MarketPlace/client
npm install    # само при първо инсталиране
npm run dev
```

Клиентът ще започне на порт 8080 (или друг свободен порт).

### Готово! 🎉

Отвори браузъра и отиди на `http://localhost:5173`

## 📁 Структура на проекта

```
MarketPlace/
├── client/                 # React frontend приложение
│   ├── src/
│   │   ├── pages/         # Страници на приложението
│   │   │   ├── Home.jsx              # Начална страница
│   │   │   ├── Catalog.jsx           # Каталог с всички обяви
│   │   │   ├── ListingDetails.jsx    # Детайли за една обява
│   │   │   ├── CreateListing.jsx     # Създаване/редактиране на обява
│   │   │   ├── MyListings.jsx        # Моите обяви
│   │   │   ├── Favorites.jsx         # Любими обяви
│   │   │   ├── Profile.jsx           # Профил на потребителя
│   │   │   └── Login.jsx             # Вход/Регистрация
│   │   ├── components/    # Общи компоненти
│   │   │   ├── Layout.jsx            # Основен layout с навигация
│   │   │   └── ErrorBoundary.jsx     # Обработка на грешки
│   │   ├── contexts/      # React Context API
│   │   │   └── AuthContext.jsx       # Контекст за автентификация
│   │   └── utils/         # Помощни функции
│   │       ├── favorites.js          # Управление на любими (localStorage)
│   │       └── seedData.js           # Инициализация на данни
│   └── package.json
│
└── server/                 # Node.js backend сървър
    ├── server.js          # Главният сървър файл
    └── data/              # JSON файлове с данни
        ├── users.json     # Потребители
        ├── listings.json  # Обяви
        └── comments.json  # Коментари
```

## 🔐 Автентификация

### Тестови потребители

В проекта има 3 готови потребителя:

| Email | Парола | Роля |
|-------|--------|------|
| peter@abv.bg | 123456 | Обикновен потребител |
| george@abv.bg | 123456 | Обикновен потребител |
| admin@abv.bg | admin | Администратор |

### Регистрация

Можеш да създадеш нов потребител от страницата за вход/регистрация.

### Как работи автентификацията

1. При вход се генерира `accessToken`
2. Token се запазва в `localStorage`
3. При всяка заявка се изпраща в header: `X-Authorization: <token>`
4. Сървърът валидира token и идентифицира потребителя

## 📝 Основни функционалности

### 1. Преглед на обяви (Catalog)
- Виждане на всички обяви
- Филтриране по категория
- Търсене по заглавие и описание
- Пагинация (9 обяви на страница)
- Добавяне/премахване от любими

### 2. Детайли на обява
- Пълна информация за обявата
- Коментари
- Бутон за любими
- Редактиране/изтриване (само за собственика)

### 3. Създаване на обява
- Задължителни полета: заглавие, описание, цена, категория
- Опционално: URL на изображение
- Автоматично присвояване на собственик (_ownerId)

### 4. Моите обяви
- Списък с всички обяви на текущия потребител
- Възможност за редактиране и изтриване

### 5. Любими обяви
- Системата използва **localStorage** за запазване на любими
- Данните се запазват локално в браузъра
- Всяко устройство има свои собствени любими

### 6. Профил
- Преглед на информацията за потребителя


### 7. Коментари
- Добавяне на коментари към обяви
- Изтриване на собствени коментари

## 🗄️ Данни и API

### Колекции

#### Listings (Обяви)
```json
{
  "_id": "уникален-id",
  "title": "Заглавие на обявата",
  "description": "Описание",
  "price": 100,
  "category": "Electronics",
  "imageUrl": "https://...",
  "location": "Град",
  "_ownerId": "user-id",
  "_createdOn": 1234567890,
  "likes": ["user1", "user2"]
}
```

#### Comments (Коментари)
```json
{
  "_id": "уникален-id",
  "listingId": "id-на-обявата",
  "text": "Текст на коментара",
  "authorEmail": "email@example.com",
  "_ownerId": "user-id",
  "_createdOn": 1234567890
}
```

#### Users (Потребители)
```json
{
  "_id": "user-id",
  "email": "email@example.com",
  "username": "Име",
  "hashedPassword": "хеширана-парола",
  "_createdOn": 1234567890
}
```

### API Endpoints

#### Потребители
- `GET /users/me` - Вземи информация за текущия потребител
- `POST /users/register` - Регистрация
- `POST /users/login` - Вход
- `GET /users/logout` - Изход

#### Обяви
- `GET /data/listings` - Вземи всички обяви
- `GET /data/listings/:id` - Вземи конкретна обява
- `POST /data/listings` - Създай нова обява (изисква автентификация)
- `PUT /data/listings/:id` - Обнови обява (само собственик)
- `PATCH /data/listings/:id` - Частично обновяване (само собственик)
- `DELETE /data/listings/:id` - Изтрий обява (само собственик)

#### Коментари
- `GET /data/comments?where=listingId="..."` - Вземи коментари за обява
- `POST /data/comments` - Добави коментар (изисква автентификация)
- `DELETE /data/comments/:id` - Изтрий коментар (само собственик)

## ❤️ Система за любими (Favorites)

### Как работи сега

използва **localStorage**:

```javascript
// Запазване на любими
localStorage.setItem('favorites_user1', JSON.stringify(['listing1', 'listing2']))

// Четене на любими
const favorites = JSON.parse(localStorage.getItem('favorites_user1'))
```

**Предимства:**
- Не изисква промени по сървъра
- Работи веднага
- Бързо

**Недостатъци:**
- Данните са само на текущото устройство
- Не се синхронизират между устройства
- Могат да се изгубят при изчистване на cache

### Как би трябвало да работи (подобрена версия)

За production би било по-добре да се използва отделна колекция `likes`:

```json
{
  "_id": "like-id",
  "listingId": "id-на-продукта",
  "userId": "id-на-потребителя",
  "_ownerId": "id-на-потребителя",
  "_createdOn": 1234567890
}
```


## 🎨 Технологии

### Frontend
- **React 18** - UI библиотека
- **React Router** - Рутиране
- **Material-UI (MUI)** - UI компоненти
- **Vite** - Build tool
- **LocalStorage API** - Локално съхранение

### Backend
- **Node.js** - Сървърна среда
- **HTTP модул** - Сървър
- **File System (FS)** - Четене/запис на JSON файлове
- **Crypto** - Хеширане на пароли

## 🔧 Разработка

### Добавяне на нова страница

1. Създай файл в `client/src/pages/`
2. Добави route в `client/src/App.jsx`
3. Добави линк в `client/src/components/Layout.jsx` (ако е нужно)

### Добавяне на нова API endpoint

Промени `server/server.js` и добави нов endpoint в съответния service.

### Промяна на стилове

Проектът използва Material-UI теми. Промени темата в `App.jsx`.

## 📚 Полезни команди

```bash
# Инсталиране на dependencies (клиент)
cd client
npm install

# Стартиране на development сървър (клиент)
npm run dev

# Build за production (клиент)
npm run build

# Проверка на код (клиент)
npm run lint

# Стартиране на сървър
cd server
node server.js
```

## 🐛 Отстраняване на проблеми

### Сървърът не стартира
- Провери дали порт 3030 е свободен
- Увери се, че имаш Node.js инсталиран

### Клиентът не може да се свърже със сървъра
- Провери дали сървърът работи на порт 3030
- Провери `API_URL` в клиентските файлове (трябва да е `http://localhost:3030`)

### Грешки при автентификация
- Провери дали token е валиден
- Провери дали потребителят съществува в `server/data/users.json`

## 📝 Бележки

- Проектът е създаден за учебни цели (SoftUni defense)
- Данните се съхраняват в JSON файлове (не е подходящо за production)
- Паролите се хешират, но използват прост алгоритъм (не е production-ready)
- Системата за любими използва localStorage (не се синхронизира между устройства)

## 👤 Автор

Създадено като проект за SoftUni defense.

## 📄 Лиценз

Този проект е за учебни цели.
